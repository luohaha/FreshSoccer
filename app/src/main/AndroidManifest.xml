<?xml version="1.1" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.freshsoccernews" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

        </activity>
        <activity android:name=".ContentActivity.ContentActivity">

        </activity>
        <activity android:name=".DetailActivity.DetailActivity">

        </activity>
        <provider
            android:authorities="com.example.root.freshsoccernews.DbModule.SinaPage.SinapageProvider"
            android:name=".DbModule.SinaPage.SinapageProvider"
            />
        <provider
            android:authorities="com.example.root.freshsoccernews.DbModule.HupuPage.HupupageProvider"
            android:name=".DbModule.HupuPage.HupupageProvider"
            />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
</manifest>
